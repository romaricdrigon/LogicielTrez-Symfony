{% macro untrim_number(number, figures) %}
    {% if number == 0 %}
        {{ number }}
    {% elseif number|length > figures %}{# >= not to create a trap #}
        {{ number }}
    {% else %}
        {{ _self.untrim_number('0'~number, figures-1) }}
    {% endif %}
{% endmacro %}
